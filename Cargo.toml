[package]
name = "cloud-chamber-firmware"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "cloud_chamber_firmware"

[dependencies]
# HAL RP2040 — Rust stable, pas d'Embassy
rp2040-hal    = { version = "0.10", features = ["rt", "critical-section-impl"] }
rp2040-boot2  = "0.3"

# 1-Wire / DS18B20 (même crate que le projet partenaire)
onewire = "0.4"

# USB CDC série sans Embassy
usb-device  = "0.3"
usbd-serial = "0.2"

# Traits HAL embarqués (bloquants)
embedded-hal = "1.0"

# Cortex-M runtime
cortex-m    = { version = "0.7" }
cortex-m-rt = "0.7"

# Synchronisation no_std (remplace embassy_sync)
critical-section = "1.1"

# Utilitaires
heapless   = "0.8"
defmt      = "0.3"
defmt-rtt  = "0.4"
panic-halt = "0.2"

mipidsi           = "0.9"
embedded-graphics = "0.8"
embedded-hal-bus  = "0.2"
portable-atomic   = { version = "1", features = ["critical-section"] }
libm              = "0.2"

[profile.release]
debug     = 2
lto       = true
opt-level = "s"
